# State -- 2026-08-27
**Phase:** Active development -- verification stack (/verify-math, /verify-model, jr_optlib), review tooling, and per-project tool integration; /portfolio skill still pending
**Last session:** Established QGIS as a per-project tool rather than a project type. Added `helpi 28` (`scripts/add_qgis.ps1`), which discovers the QGIS install at run time and writes workspace-scoped `.vscode/settings.json`, `.env` and `qgis_smoketest.py` into whichever project needs maps. Recorded the convention and the three PyQGIS traps as known_issues #50, then dissolved `JR\QGIS_dev\`. Also ran /review-paper live for the first time (JCYMR-D-26-00038, major revision, files in `JR\Reviews\2026_JCYMR_Gao\`).
**Next:** (1) merge and push `feat/helpi-28-pyqgis` (08aab17, one commit ahead of master, currently the checked-out branch); (2) `helpi 16` so command 28 reaches infrastructure.html; (3) record the `gpd.datasets.get_path` deprecation at `Pub_StopGeometry_TBA\Code\visualize.py:393` in that project -- fig3 may be silently skipping; (4) `JR\Reviews\_voice\` is absent although the previous session logged creating it, so review-letter voice calibration is unstarted. Carried over: calibrate the /review-paper rubric against a paper already decided; decide whether /review-paper belongs in infrastructure.html; trim verification deck for a 25-min slot; reconcile jr_optlib doc counts (functions.yaml 40 vetted + 2 experimental vs README 41/139 vs INDEX.md 45 rows, actual tests 143); /portfolio skill; /close updates status line; helpi 10/11 auto-bump status; helpi 13 pipeline view; rewire papers to jr_optlib; VERIFICATION.md; gurobi.lic.
**Git ref:** 08aab17
**Agent:** Claude Opus 5
