# State -- 2026-09-03
**Phase:** Active development -- verification stack (/verify-math, /verify-model, jr_optlib), review tooling, and per-project tool integration; /portfolio skill still pending
**Last session:** Voice input enabled in the CLI. No plugin was needed or possible: dictation is native (`/voice`, hold or tap), and plugins run only after a prompt is submitted so none can reach a microphone. The actual fault was a device one, Windows had made a webcam mic the system default while the laptop array mic sat active and unused, which degrades transcription in a way that reads as a model failure. Switched the default via `IPolicyConfig` across all three roles and confirmed clean transcription. Recorded as known_issues #53, together with the auth requirements (Claude.ai account only, not API key or Bedrock) and two measurement traps: `Role:*` registry keys are not written on Windows 11, and the WASAPI peak meter reads 0.0 on an idle endpoint.
**Next:** New and optional: pin `"voice": { "enabled": true, "mode": "tap" }` in `~/.claude/settings.json`, and plug in the AKG Ara USB mic for long dictation. Carried unchanged from 2026-08-27: (1) merge and push the branch -- now 5+ commits ahead of master, still misnamed `feat/helpi-28-pyqgis`, consider `feat/infra-tooling`; (2) **correctness item** -- pin the solver backend explicitly in `Pub_MIPEntropy_MPC`, `Pub_PMIP_AOR`, `Pub_SAA_PMIP_MC`: ortools is imported at 68 sites behind a silent PuLP fallback and installed nowhere, so installing it would change three papers' solver without warning (known_issues #45); `Pub_MIPEntropy_MPC/requirements.txt` already declares ortools, so the declared environment diverges from the actual one; (3) `install.packages(c("minpack.lm","openxlsx","reshape2"))` -- used but missing, `Pub_NonlinearDiffusion_PartB`; (4) `helpi 16` so commands 28/29 reach infrastructure.html; (5) pandoc for the .docx referee workflow; (6) dataflow pipeline visualiser prototype, design in `IDEAS.md` #1; (7) the `Stop` auto-commit hook still does `git add -A`, see known_issues #52, removal is manual. Optional: trial VS Code extension `nuriyev.claude-code-katex`, or build the KaTeX side-pane, for true glyph rendering. Carried over: `gpd.datasets.get_path` deprecation at `Pub_StopGeometry_TBA\Code\visualize.py:393`, fig3 may be silently skipping; `JR\Reviews\_voice\` absent though logged as created, voice calibration unstarted; calibrate the /review-paper rubric against a decided paper; trim verification deck for a 25-min slot; reconcile jr_optlib doc counts; /portfolio skill; /close updates status line; helpi 10/11 auto-bump status; helpi 13 pipeline view; rewire papers to jr_optlib; VERIFICATION.md; gurobi.lic.
**Git ref:** 47fa1a7
**Agent:** Claude Opus 5
