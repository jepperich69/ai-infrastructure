# State -- 2026-09-03
**Phase:** Active development -- verification stack (/verify-math, /verify-model, jr_optlib), review tooling, and per-project tool integration; /portfolio skill still pending
**Last session:** Made terminal math legible in two tiers. Installed the third-party `claude-math` plugin (model-invoked skill, inline Unicode glyphs, no executables) and built a SymPy 2D pretty-printer at `~\.claude\skills\verify-math\scripts\render_math.py` for displayed equations, wired into /verify-math so reports and failures show rendered mathematics rather than SymPy source. Global CLAUDE.md now carries a "Math display in the terminal" rule so the split does not depend on skill-invocation judgement. Confirmed inline glyph rendering is impossible in this TUI (the repaint clobbers Sixel/kitty sequences); out-of-band rendering is the only route and is unbuilt.
**Next:** Carried unchanged from 2026-08-27: (1) merge and push the branch -- now 4 commits ahead of master, still misnamed `feat/helpi-28-pyqgis`, consider `feat/infra-tooling`; (2) **correctness item** -- pin the solver backend explicitly in `Pub_MIPEntropy_MPC`, `Pub_PMIP_AOR`, `Pub_SAA_PMIP_MC`: ortools is imported at 68 sites behind a silent PuLP fallback and installed nowhere, so installing it would change three papers' solver without warning (known_issues #45); `Pub_MIPEntropy_MPC/requirements.txt` already declares ortools, so the declared environment diverges from the actual one; (3) `install.packages(c("minpack.lm","openxlsx","reshape2"))` -- used but missing, `Pub_NonlinearDiffusion_PartB`; (4) `helpi 16` so commands 28/29 reach infrastructure.html; (5) pandoc for the .docx referee workflow; (6) dataflow pipeline visualiser prototype, design in `IDEAS.md` #1. New and optional: trial VS Code extension `nuriyev.claude-code-katex`, or build the KaTeX side-pane, for true glyph rendering. Carried over: `gpd.datasets.get_path` deprecation at `Pub_StopGeometry_TBA\Code\visualize.py:393`, fig3 may be silently skipping; `JR\Reviews\_voice\` absent though logged as created, voice calibration unstarted; calibrate the /review-paper rubric against a decided paper; trim verification deck for a 25-min slot; reconcile jr_optlib doc counts; /portfolio skill; /close updates status line; helpi 10/11 auto-bump status; helpi 13 pipeline view; rewire papers to jr_optlib; VERIFICATION.md; gurobi.lic.
**Git ref:** 7e974e0
**Agent:** Claude Opus 5
