# State -- 2026-08-27
**Phase:** Active development -- verification stack (/verify-math, /verify-model, jr_optlib), review tooling, and per-project tool integration; /portfolio skill still pending
**Last session:** Installed Biogeme 3.3.4 in a venv outside OneDrive and validated it against the published Swissmetro MNL. Made tool tracking generated rather than hand-maintained: `helpi 29` (`scripts/tool_inventory.ps1`) probes 26 tools and rewrites `TOOLS.md`, replacing three hand-written lists that had drifted apart. Rewrote `INSTALL.md` Part B into four tiers carrying the install traps. Confirmed the profiling stack and documented it in `PROFILING.md`, pointed at from global CLAUDE.md so a session does not reach for cProfile and misattribute native time. Surveyed all 113 projects for dependency gaps.
**Next:** (1) merge and push the branch -- 3 commits ahead of master, still named `feat/helpi-28-pyqgis` although it now carries tool inventory, profiling and Biogeme, so consider renaming to `feat/infra-tooling`; (2) **correctness item** -- pin the solver backend explicitly in `Pub_MIPEntropy_MPC`, `Pub_PMIP_AOR`, `Pub_SAA_PMIP_MC`: ortools is imported at 68 sites behind a silent PuLP fallback and is installed nowhere, so installing it would change three papers' solver without warning (same hazard as known_issues #45); `Pub_MIPEntropy_MPC/requirements.txt` declares ortools, so the declared environment already diverges from the actual one; (3) `install.packages(c("minpack.lm","openxlsx","reshape2"))` -- used but missing, `Pub_NonlinearDiffusion_PartB`; (4) `helpi 16` so commands 28/29 reach infrastructure.html; (5) pandoc for the .docx referee workflow; (6) dataflow pipeline visualiser prototype -- full design in `IDEAS.md` #1. Carried over: `gpd.datasets.get_path` deprecation at `Pub_StopGeometry_TBA\Code\visualize.py:393`, fig3 may be silently skipping; `JR\Reviews\_voice\` absent though logged as created, voice calibration unstarted; calibrate the /review-paper rubric against a decided paper; trim verification deck for a 25-min slot; reconcile jr_optlib doc counts; /portfolio skill; /close updates status line; helpi 10/11 auto-bump status; helpi 13 pipeline view; rewire papers to jr_optlib; VERIFICATION.md; gurobi.lic.
**Git ref:** 59176e1
**Agent:** Claude Opus 5
