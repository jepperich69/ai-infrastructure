# State -- 2026-09-04
**Phase:** Active development -- verification stack (/verify-math, /verify-model, jr_optlib), review tooling, and per-project tool integration; /portfolio skill still pending
**Last session:** Answer-length discipline is now a standing rule for all three CLIs. Global `~\.claude\CLAUDE.md` gained an "Answer length and drill-downs" section (conclusion first, under ~150 words of prose, max 5 one-line bullets, cite `file.py:42` rather than pasting code, report results not process, close with one optional `Drill down: (a)... (b)... (c)...` line; full length only for written deliverables, explicit requests for detail, and irreversible actions). The same rule was injected into `scripts\generate_handover.ps1` so it lands in every generated AGENTS.md, and all 41 project AGENTS.md files under `JR\` were regenerated, giving Codex and Gemini the rule everywhere. Regeneration called the generator directly, not `helpi 7`, so no session logs were written and no browsers opened.
**Next:** New and optional: add the answer-length rule to the per-project `.claude/CLAUDE.md` template so new projects carry it at creation; note AI_auto itself has no `.claude/CLAUDE.md`. New latent bug, pre-existing and untouched: `scripts\generate_handover.ps1:200` has an em-dash inside a PowerShell string literal, the encoding trap the global platform rules warn about. Carried unchanged: (1) merge and push the branch -- now 6+ commits ahead of master, still misnamed `feat/helpi-28-pyqgis`, consider `feat/infra-tooling`; (2) **correctness item** -- pin the solver backend explicitly in `Pub_MIPEntropy_MPC`, `Pub_PMIP_AOR`, `Pub_SAA_PMIP_MC`: ortools is imported at 68 sites behind a silent PuLP fallback and installed nowhere, so installing it would change three papers' solver without warning (known_issues #45); `Pub_MIPEntropy_MPC/requirements.txt` already declares ortools, so the declared environment diverges from the actual one; (3) `install.packages(c("minpack.lm","openxlsx","reshape2"))` -- used but missing, `Pub_NonlinearDiffusion_PartB`; (4) `helpi 16` so commands 28/29 reach infrastructure.html; (5) pandoc for the .docx referee workflow; (6) dataflow pipeline visualiser prototype, design in `IDEAS.md` #1; (7) the `Stop` auto-commit hook still does `git add -A`, see known_issues #52, removal is manual. Optional: pin `"voice": { "enabled": true, "mode": "tap" }`, plug in the AKG Ara USB mic for long dictation, trial VS Code extension `nuriyev.claude-code-katex` or build the KaTeX side-pane. Carried over: `gpd.datasets.get_path` deprecation at `Pub_StopGeometry_TBA\Code\visualize.py:393`, fig3 may be silently skipping; `JR\Reviews\_voice\` absent though logged as created, voice calibration unstarted; calibrate the /review-paper rubric against a decided paper; trim verification deck for a 25-min slot; reconcile jr_optlib doc counts; /portfolio skill; /close updates status line; helpi 10/11 auto-bump status; helpi 13 pipeline view; rewire papers to jr_optlib; VERIFICATION.md; gurobi.lic.
**Git ref:** e79b5a2
**Agent:** Claude Opus 5
